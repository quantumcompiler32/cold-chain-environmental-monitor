.venv/
__pycache__/
*.py[cod]
.DS_Store
*.log
.dashboard_uploads/
test-reports/

# Keep the optional AI artifacts in the handoff so another checkout can run
# the inference tab without retraining first. Python caches remain ignored.

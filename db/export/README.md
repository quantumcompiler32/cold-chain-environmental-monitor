# Database exports

ML-compatible export and retraining are intentionally deferred from the current
implementation slice. This directory is reserved for the checked-in export
contract and deterministic exporter when that work begins.
